<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!--
        Permissions.

        The WebView cannot grant itself anything the app has not declared. Capacitor's
        BridgeWebChromeClient maps a getUserMedia request onto the matching Android permission and
        asks for it at the moment of use - but if the permission is missing from this file the
        request is refused outright, with no prompt and no error the page can see. That is exactly
        how camera and microphone came to do nothing at all: the page asked, Android declined
        silently, and the operator saw a dead button.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Photographing a reading or a fault, and recording video evidence. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Voice notes, and the audio track of a recorded video. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Stamping where a reading was taken. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
        Identifying an asset by its tag. NFC is a normal permission - it is granted at install and
        never prompts - so the reader either works or the hardware is absent.
    -->
    <uses-permission android:name="android.permission.NFC" />

    <!--
        Declared but not required, every one of them. A hardware feature used through a permission
        is implicitly "required" unless said otherwise, and a required feature keeps the app off
        every device that lacks it. Some tablets in this plant have no NFC and some have no rear
        camera; they must still be able to open a log sheet and type readings in. The app asks at
        the point of use and copes with "no" - see isNFCSupported() and the media capture guards.
    -->
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
</manifest>
