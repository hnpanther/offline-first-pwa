# Dependencies
node_modules/

# Build & PWA dev service worker cache
dist/
dev-dist/

# Local TLS (mkcert) — never commit; created by npm run setup:mkcert
certs/

# AI
.claude/

# Environment — machine-specific URLs/secrets
.env
.env.local
.env.*.local
.env.mobile

# TypeScript
*.tsbuildinfo

# Test coverage
coverage/

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Linters / caches
.eslintcache
.cache/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor (keep shared workspace settings if present)
.idea/
*.sw?
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Android — the site CA is copied in from certs/ by npm run ca:apk, never committed
android/app/src/main/res/raw/plant_ca.crt
